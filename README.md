# 2048 AI
这是一个基于 **N-tuple Network + TD-Learning + Expectimax Search** 算法的 2048 AI 项目。

## 测试数据 (Performance)
本项目在多次 10000 局连续测试中交出了极为优异的成绩，测试数据见下表。注：评测机器的CPU为 `i9 14900HX`，自适应 5 层搜索指的是若盘面上不小于 256 的不同的数至少有 5 个则使用 5 层搜索，反之使用 4 层搜索。
*注：在 `main_gui.py` 的可视化界面中，受渲染帧数影响速度约为 220 steps/s（4 层搜索）。*

| 常见指标 | 3 层搜索 (depth=2) | 4 层搜索 (depth=3) | 自适应 5 层搜索 (depth=4) |
| --- | --- | --- | --- |
| 测试局数 | 10000 局 | 10000 局 | 10000 局 |
| 运行速度 (steps/s) | ~ 5400 | ~ 800 | ~ 400 |
| 平均分 | 539449 | 568017 | **578585** |
| 最低分 | 7740 | **32148** | 17148 |
| 最高分 | 824996 | **830412** | 829312 |
| 8192 成功率 | 99.75% | **99.84**% | 99.83% |
| 16384 成功率 | 95.60% | 96.67% | **96.94**% |
| 32768 成功率 | 53.85% | 59.86% | **61.82**% |
| 超高分(>800000) | 10.29% | 13.77% | **14.86**% |
---

## ⚠️ 重要提示：Git LFS (Large File Storage)
本项目包含超过 500MB 的训练模型（ `.bin` 权重文件）。普通的 `git clone` **无法**下载完整的模型文件，会导致 AI 乱玩或报错！
在克隆本仓库之前，请务必安装并初始化 Git LFS：

```bash
git lfs install
git clone https://github.com/Alex426250/2048_ai.git
```

---

## 快速开始 (Quick Start)

### 1. 编译 C++ 核心加速库
进入 `TD-Learning+N-tuple Network` 目录，并编译不同阶段的 AI 引擎（请确保系统已安装 `g++` 并支持 OpenMP）：

```bash
cd "TD-Learning+N-tuple Network"

# 编译 base_agent
g++ -O3 -shared -fPIC -march=native -fopenmp base_agent/base_agent.cpp -o base_agent/base_agent.dll

# 编译 8k4k_agent
g++ -O3 -shared -fPIC -march=native -fopenmp 8k4k_agent/8k4k_agent.cpp -o 8k4k_agent/8k4k_agent.dll

# 编译 16k8k4k_agent
g++ -O3 -shared -fPIC -march=native -fopenmp 16k8k4k_agent/16k8k4k_agent.cpp -o 16k8k4k_agent/16k8k4k_agent.dll
```
*注：Linux/macOS 用户需将输出后缀 `.dll` 改为 `.so`，并在对应的 python 脚本中修改加载路径。*

### 2. 运行脚本
编译完成后，你可以在 `TD-Learning+N-tuple Network` 目录运行以下脚本体验被 AI 统治的 2048：
- `python main_gui.py`: 启动可视化界面模式（支持自定义盘面、前进/后退、速度调节）。
- `python measure_10000_games.py`: 连续运行 10000 局并统计胜率和分数。
- `python measure_speed.py`: 测试单局运行时间与步数速度。
- `python base_agent/base_agent.py`: 训练 `base` 模型。
- `python 8k4k_agent/8k4k_agent.py`: 训练 `8k4k` 模型。
- `python 16k8k4k_agent/16k8k4k_agent.py`: 训练 `16k8k4k` 模型。

*注：如果想从头开始训练模型，需要先删除已有的模型。*

---

## 核心算法思想 (Algorithm)

### N-tuple Network
如果不考虑空间大小，使用暴力 DP 能得到最优解，但状态数 $16^{16}$ 过于庞大（假设每个格子都不超过 32768）。所以这个算法的核心是将盘面拆分成若干个 **Feature**。每个 Feature 为 16 个格子的一个子集（大小足够小，可以存入内存中），并以此训练出一个状态分数（score）。
该盘面的总 score 即为所有 Feature 的 score 之和。

### TD-Learning (时序差分)
模型使用时序差分更新，AI 每走一步都会更新一次参数。
假设该步动作的直接得分为 $inc$，动作前后的盘面 score 分别为 $s_1, s_2$。系统会给动作前盘面的每个 Feature 的 score 加上 $\alpha\cdot (s_2+inc-s_1)$。这里 $\alpha$ 为一个极小的学习率常数，代码里取值 $\alpha=0.001$。

### Expectimax Search (期望最大值搜索)
给定搜索深度 `depth`，计算目前盘面往四个方向走一步之后，再经过 `depth` 回合内（每步都走最优解）的期望分数（递归到 `depth=0` 时直接调用盘面模型估测的 score），最后取期望分数最大的 Action 作为当前动作。
显然，`depth` 越大模型越强，但由于每次落子都会产生随机块，搜索树大小呈指数级爆炸，速度会下降很多。

---

## Multi-Stage 多阶段训练策略
本项目使用了**多阶段训练方法**，将一局 2048 游戏拆分成了三个阶段（定义盘面上 $\ge 4096$ 的数为“大数”，设其数量为 $A$）：
- **$A \le 1$**：使用 `base_agent` 模型。
- **$A = 2$**：使用 `8k4k_agent` 模型。
- **$A \ge 3$**：使用 `16k8k4k_agent` 模型。
*(注：在训练 16k8k4k 模型时只考虑了 $A=3$ 的情形，因此当盘面同时出现 32768, 16384, 8192, 4096 时 AI 会陷入混乱。这也是为什么目前最高分大约止步于 830,000 左右)。*

---

## 模型与特征设计 (Model Details)
模型总大小约 **510MB**。具体设计细节如下：

### 1. Tuple 设计
假设盘面 16 个格子编号依次为 0-15：
- **`base` 模型**使用了 6 个 6-tuple：`{0,1,2,3,4,5}`, `{4,5,6,7,8,9}`, `{8,9,10,11,12,13}`, `{0,1,2,4,5,6}`, `{4,5,6,8,9,10}`, `{0,1,2,3,4,8}`。
- **`8k4k` 模型和 `16k8k4k` 模型**多使用了一个 6-tuple，共计 7 个：增加了 `{4,5,6,7,8,12}`（L形阵形）。
- 每个 Feature 取自所有 tuple 经过盘面旋转、翻转后的所有组合。由于旋转和翻转不影响策略逻辑，相同派生 tuple 在内存中共享同一块地址，得分也完全一致。

**设计合理性：**
目前网上的 AI 使用的阵形大多数为 `628` 或 `63L`。作者查阅胜率数据后发现，在 32k 残局 (final 512) 中，蛇形层叠阵形的胜率并不逊色，因此围绕**层叠阵形**进行 tuple 设计：

- 前三个 tuple 是最为标准的层叠形状。
- 后两个矩形 tuple 主要起到**防滑作用**。（例如某行是 `2048,1024,32,16`，防滑设计能引导其合并为 `2048,1024,64,16` 而非错误的 `...32,32`）
- L 形 tuple 是为了学习**滑角后的救场机制**。（例如角落的 `64` 滑出变成 `2048,1024,256,32,0,0,64,2` ，此时最优策略就是从 `2` 的下面继续合成） 在第三个阶段中，当第一行被大数填满时，盘面仅剩 3 行进行游戏，两个 L 形的存在能显著提升极端情况的存活率。

### 2. 模型目标、初始状态与终止截断
所有模型均存储在各自 Feature 对应的 score 数组中，初始化参数全为 0。为了防止 AI "死在莫名奇妙的地方导致负向更新"（例如专门训练两大数残局的 8k4k 模型却死在了 16k 加上一堆小数里），我们在训练中引入了**合成大数截断机制**，实测提升显著。

| 模型 | 训练目标 | 初始盘面分布 | 终止条件 (截断) |
| --- | --- | --- | --- |
| **Base** | 从 0 到 4k，以及单大数环境下，再合出 4k | 标准 2048 初始盘面 (两个 2 或 4) | **合出 8k**，或正常死亡 |
| **8k4k** | 两个不同大数环境下，再合出 4k | 90% 概率角落排布(16k, 4k，其他 $\le 16$)；10% 随机排布(训练乱阵归位能力) | **合出 16k+8k**，或正常死亡 |
| **16k8k4k**| 三个不同大数环境下，再合出 4k | 90% 概率角落排布(32k, 16k, 4k，其他 $\le 16$)；10% 随机排布(训练乱阵归位能力) | **合出 32k+16k+8k**，或正常死亡 |

### 3. 训练细节秘籍
1. **训练参数**：采用阶段性学习率（$\alpha$）衰减策略。
   | 模型 | 总训练局数 | 阶段 1 ($\alpha=0.001$) | 阶段 2 ($\alpha=0.0002$) | 阶段 3 ($\alpha=0.00004$) | 阶段 4 ($\alpha=0.00001$) |
   | --- | --- | --- | --- | --- | --- |
   | **Base** | 50M 局 | 前 40M 局 | 后 8M 局 | 后 1.6M 局 | 最后 0.4M 局 |
   | **8k4k** | 75M 局 | 前 60M 局 | 后 12M 局 | 后 2.4M 局 | 最后 0.6M 局 |
   | **16k8k4k** | 100M 局 | 前 80M 局 | 后 16M 局 | 后 3.2M 局 | 最后 0.8M 局 |
   
2. **训练速度**：在 `i9 14900HX` CPU下约为 `1200000 steps/s`。不同模型训练时长不一样，且由于训练时模型越来越强，每一局的时间也会变长。经过测试，在同样CPU下， `base` 模型训练 10000 局最慢时间约为 32 秒，`8k4k` 模型训练 10000 局最慢时间约为 16 秒，`16k8k4k` 模型训练 10000 局最慢时间约为 14 秒。因此，完成所有训练只需要两天不到的时间。*(注：为了防止不同核心更新时冲突，我们采用的是单核训练)。*

3. **初始盘面**：每次生成 1000 个初始盘面供随机抽取，每打 1 万局剔除并换新 2 个盘面（有效防止过拟合）。

4. **特殊判断**：在大数合并阶段（单步分数大于 4000 分）尽量保证最大数在角落，防止可能的大数归位损失成功率。

5. **最后一步更新**：在面对合出大数而截断的最后一步更新时，我们将**最后截断状态“降维”后的score加上该步直接得分作为其估值**（把 8192 降级视作 4096），因为终止状态在降维后就等价于该模型训练时的初始盘面，所以能非常自然、合理地得到分数。

6. **模型极致压缩 (进制缩维)**：
   - **Base (~110MB)**：盘面上 $\ge 4096$ 的数没有本质区别，最高统计到 4096。将 16 进制缩减至 **13 进制**，参数量从 $6\times 16^6$ float暴降至 $6\times 13^6$ float。
   - **8k4k (~200MB)**：盘面上 $\ge 8192$ 的数无区别，采用 **14 进制**。*(注：故意保留了 4096 与 8192 的区分度，防止 AI 利用对称性将 4096 和 8192 强行放在第一行的中间而非角落)。*
   - **16k8k4k (~200MB)**：同理采用 **14 进制**。$16384$ 与 $8192$ 则因其位置绝对固定在角落（0 和 1），包含大数的 tuple 在翻转/旋转下不会重合，因此无需拆分。

---

## 程序加速与性能优化 (Optimization)
本项目主要进行了四维度的底层优化：

1. **Bitboard & 查表法**
   不考虑极端的 65536 块，每个格子有 16 种状态，正好占 `4-bit`。
   整个 4x4 盘面直接压缩成一个 64 位的 `unsigned long long`。
   预先计算出单行（共 $16^4 = 65536$ 种组合）的左右移动位移变换和得分增量，用空间换时间。这使得对盘面进行一轮拨动只需查表 **4** 次。
2. **哈希去重 (Transposition Table)**
   在 Expectimax 展开搜索树中，"先上后左" 和 "先左后上" 往往导致相同的盘面结果。利用 Hash 去重有效剪枝。
3. **四路并行展开 (Multi-Threading)**
   针对于对弈树的根节点（即真实操控的四个方向步），利用 OpenMP 开启 4 个线程并行递归搜索。
4. **无锁哈希表 (Lock-Free Transposition Table)**：与传统的加锁 (Mutex) 不同，程序使用了高阶的 C++ `std::atomic` 原子操作和 `std::memory_order_relaxed` 内存序来进行并发读写。在保证线程安全不冲突的同时，完全消除了锁竞争开销，完美榨干现代 CPU 的多核性能。

加上以上四个优化后，在四层搜索下（depth=3），运行速度从 **100** steps/s 提升到了 **800** steps/s。

## 本项目可改进之处

1. 本项目仅尝试了部分学习率，所以我们采用的学习率不一定最优，或许有更好的学习率能学出更好的模型。
2. 本项目每个阶段以合出 4096 为最小单位，步数较多，将 4096 改成更小的数（例如 2048）说不定能学出更好的模型。
3. 本项目模型初始化均为 0，可以采用 **Optimistic TD-Learning** 将所有score初始化成一个很大的数（或者提高死亡惩罚，容易证明这两者在数学上等价）鼓励模型探索未知盘面，可能会有更好的效果。
4. 评估时直接把所有 Feature 的分数加起来作为盘面的估值，这个近似函数不一定准确，一是因为线性系数不一定要全是 1，二是因为线性函数忽略了不同 Feature 之间的关系，所以面对复杂的情况不一定能走出最优解。
5. 本 AI 只会玩层叠阵形，对灭顶，滑出等情况抗性较低，可以尝试更换 Feature 学习更高级的阵形（例如 `628`，`63L` 甚至是T阵）。
6. 本项目还没训练四大数残局的模型（32k16k8k4k），导致最高分止步 830000 左右，未来可以补上这一部分使得模型能合出 65536 方块（预计概率 0.5% ~ 1%）。
7. 本项目底层虽然已经做了很多优化，但我觉得还有不小的改进空间，例如优化访存，增加并行计算等等。

## 参考文献

1. Hung Guei, Lung-Pin Chen, and I-Chen Wu. Optimistic temporal difference learning for 2048.
   IEEE Transactions on Games, 14(2):283–292, 2022.
2. Wojciech Jaskowski. Mastering 2048 with delayed temporal coherence learning, multi-stage
   weight promotion, redundant encoding, and carousel shaping. IEEE Transactions on Computa-
   tional Intelligence and AI in Games, 10(2):122–134, 2018.
3. Marcin Szubert and Wojciech Ja´skowski. Temporal difference learning of n-tuple networks for
   the game 2048. In 2014 IEEE Conference on Computational Intelligence and Games, pages 1–8.
   IEEE, 2014.
4. Kun-Hao Yeh, I-Chen Wu, Chu-Hsuan Hsueh, Chia-Chuan Chang, Chao-Chin Liang, and Han
   Chiang. Multi-stage temporal difference learning for 2048-like games. IEEE Transactions on
   Computational Intelligence and AI in Games, 9(4):369–380, 2017.






